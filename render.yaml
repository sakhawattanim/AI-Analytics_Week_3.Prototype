services:
  - type: web
    name: business-analytics-week3-prototype
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT